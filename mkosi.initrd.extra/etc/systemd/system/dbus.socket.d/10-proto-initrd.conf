[Unit]
# The default openSUSE initrd enables the system bus socket through a vendor
# preset, but no initrd service needs it. The main-system unit is unaffected
# because this drop-in disappears at switch-root.
ConditionPathExists=!/etc/initrd-release
